/** @type {import('tailwindcss').Config} */
module.exports = {
    prefix: '',

    darkMode: 'class',

    theme: {
        extend: {},
    },

    variants: {},

    plugins: [
        require('@tailwindcss/forms'),
    ],
}
